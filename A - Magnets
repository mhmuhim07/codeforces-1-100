#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    vector<int>s(n);
    for(int i=0;i<n;i++)cin>>s[i];
    if(n==1)cout<<"1"<<endl;
    else{

    int sz= unique(s.begin(),s.end())-s.begin();
    cout<<sz;
    }
    return 0;
}
