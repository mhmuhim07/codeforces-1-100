#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define fo(i,a,n) for(ll i=a;i<n;i++)
#define all(v) (v).begin(),(v).end()
#define test int t; cin>>t; for(int no = 1;no<=t;no++)
#define pb push_back
#define endl '\n'
#define ff first
#define ss second
#define fast ios_base::sync_with_stdio(false);cin.tie(0),cout.tie(0);

int main() {

    fast;

    int t=1;
    cin >> t;
    while(t--){
       int n;
       cin>>n;
       if(n<=2||n==4||n==5||n==8||n==11)
        cout<<"NO"<<endl;
    else cout<<"YES"<<endl;
    }
  
    return 0;
}
